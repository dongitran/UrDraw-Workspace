CREATE TABLE hieudien_test_table_14
(
    id          varchar(255) UNIQUE NOT NULL,
    name        varchar(255)        NOT NULL,
    user_id     varchar(255)        NOT NULL,

    PRIMARY KEY (id)
);
